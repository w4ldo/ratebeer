<p id="notice"><%= notice %></p>

<h2>
  <strong>Name:</strong>
  <%= @brewery.name %>
</h2>

<i>
  Established in:
  <%= @brewery.year %>
</i>

<p>
  Number of beers: 
  <%= @brewery.beers.count %>
</p>

<p>
 <% @brewery.beers.each do |beer| %>
   <%= link_to beer.name, beer %>
 <% end %>
</p>

<p>
<% if @brewery.ratings.empty? %>
    brewery has no ratings yet!
<% else %>
    Brewery has 
      <%= pluralize(
         @brewery.ratings.count,
         "rating"
       ) %>
    , average: 
    <%= @brewery.average_rating %>
<% end %>

</p>

<%= link_to 'Edit', edit_brewery_path(@brewery) %> |
